repo:
  - name: io.peasoup.public.net.http
    path: net/http
    src: https://github.com/peasoupio/inv-repo.git

    hooks:
      init: |
          git clone ${src} .

      pull: |
          git pull