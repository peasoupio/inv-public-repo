repo:
  - name: io.peasoup.inv.pub.net.http
    path: net/http
    src: https://github.com/peasoupio/inv-repo.git

    hooks:
      init: |
          git clone ${src} .

      pull: |
          git pull