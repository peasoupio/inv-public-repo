repo:
  - name: io.peasoup.inv.pub.net.ssh
    path: net/ssh
    src: https://github.com/peasoupio/inv-repo.git

    hooks:
      init: |
          git clone ${src} .

      pull: |
          git pull