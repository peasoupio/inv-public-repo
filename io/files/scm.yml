repo:
  - name: io.peasoup.public.io.files
    path: io/files
    src: https://github.com/peasoupio/inv-repo.git

    hooks:
      init: |
          git clone ${src} .

      pull: |
          git pull